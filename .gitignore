# Personal Claude Code settings — per-developer, never committed
.claude/settings.local.json
CLAUDE.local.md

# Claude Code runtime artifacts — written by hooks during a session
.claude/.tool-log
.claude/state/

# Secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*~

# Node (in case the template gets used to build something here)
node_modules/
dist/
build/
coverage/
