<!--
Memory index. One line per memory, format:  - [Title](file.md) — short hook
Loaded into Claude's context every session, so keep hooks terse.
Each memory is a sibling .md file with YAML frontmatter (name, description,
metadata.type). type ∈ user | feedback | project | reference.
See .claude/rules/project-memory.md for the full convention.
A fresh project starts with this index empty — add rows as memories accrue.
-->

# Project Memory Index
